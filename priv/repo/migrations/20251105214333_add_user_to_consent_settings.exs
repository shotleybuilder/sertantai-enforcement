defmodule EhsEnforcement.Repo.Migrations.AddUserToConsentSettings do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    alter table(:consent_settings) do
      add :user_id,
          references(:users,
            column: :id,
            name: "consent_settings_user_id_fkey",
            type: :uuid,
            prefix: "public"
          )
    end
  end

  def down do
    drop constraint(:consent_settings, "consent_settings_user_id_fkey")

    alter table(:consent_settings) do
      remove :user_id
    end
  end
end
